# Prism Web

This directory contains the Prism dashboard built with React, Umi Max, and Ant Design.

Common commands:

```bash
pnpm install
pnpm dev
pnpm build
```

For full project setup, configuration, and security guidance, see the repository root `README.md` and `SECURITY.md`.
